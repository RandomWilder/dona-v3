services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: dona
      POSTGRES_USER: dona
      POSTGRES_PASSWORD: dona
    ports:
      - '5434:5432'
    volumes:
      - dona3_pg:/var/lib/postgresql/data

volumes:
  dona3_pg:
